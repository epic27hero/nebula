apiVersion: v1
kind: Service
metadata:
  name: fastapi-app
  labels:
    app.kubernetes.io/name: fastapi-app
spec:
  type: LoadBalancer
  selector:
    app: fastapi
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
